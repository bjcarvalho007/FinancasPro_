# Project Guidelines & Rules

## Admin & User Account Rules
- The ONLY legitimate account for the owner/admin is `bjcarvalho07@gmail.com`.
- `bjcarvalho007@gmail.com` does not exist, has no account created, and must NEVER be listed as a user, admin, VIP, or referenced anywhere in code or UI.
- The ONLY user authorized to see or access the Admin Panel (`AdminPanel`, admin tab, and `isAdmin` condition) is `bjcarvalho07@gmail.com`.
